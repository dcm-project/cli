name: Tag Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  ci:
    uses: ./.github/workflows/ci.yaml

  lint:
    uses: ./.github/workflows/lint.yaml

  release:
    needs: [ci, lint]
    uses: ./.github/workflows/release.yaml
